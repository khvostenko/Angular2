import { QueryStringDeserializer } from './query.string.deserializer';
import { UserDecoder } from './user.decoder';
import { Storage } from './storage';
export declare class AadRedirectProcessor {
    private queryStringDeserializer;
    private userDecoder;
    private storage;
    private window;
    constructor(queryStringDeserializer: QueryStringDeserializer, userDecoder: UserDecoder, storage: Storage, window: Window);
    process(): boolean;
}
