export declare class QueryStringDeserializer {
    private plRegex;
    deserialize(queryString: string): any;
    private decode(s);
    private trimHashSign(hash);
}
export declare function hasAadProps(deserializedHash: any): boolean;
