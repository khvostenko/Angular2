import { GuidGenerator } from './guid.generator';
import { Storage } from './storage';
import { Navigator } from './navigator';
import { AadUrlBuilder } from './aad.url.builder';
import { UserDecoder } from './user.decoder';
import { AdalConfig } from './adal.config';
import { AadLogoutUrlBuilder } from './aad.logout.url.builder';
import { User } from './user';
export declare class AuthenticationContext {
    private idTokenNonce;
    private instance;
    private loginInProgress;
    private config;
    private storage;
    private navigator;
    private guidGenerator;
    private aadUrlBuilder;
    private userDecoder;
    private logoutUrlBuilder;
    private CONSTANTS;
    private REQUEST_TYPES;
    constructor(config: AdalConfig, storage: Storage, navigator: Navigator, guidGenerator: GuidGenerator, aadUrlBuilder: AadUrlBuilder, userDecoder: UserDecoder, logoutUrlBuilder: AadLogoutUrlBuilder);
    login(): void;
    getUser(): User;
    getToken(): string;
    getAccessToken(): string;
    logout(): void;
    private verbose(message);
    private info(message);
    private createOptions();
    private cloneConfig(obj);
}
