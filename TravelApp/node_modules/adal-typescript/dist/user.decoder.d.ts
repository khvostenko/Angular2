import { User } from './user';
export declare class UserDecoder {
    decode(encoded: string): User;
    private safeDecodeBase64(value);
    private decodeJwt;
    private base64DecodeStringUrlSafe;
    private decodeBase64(base64IdToken);
    private isEmpty(str);
}
